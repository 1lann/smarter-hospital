package logic

import "github.com/1lann/smarter-hospital/ws"

// Register registers logic components in the logic package using the
// provided WebSocket server as the place to emit events.
func Register(wsServer *ws.Server) {}
